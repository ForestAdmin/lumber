sudo: required

language: node_js

node_js: 12.13.0

env:
  - NODE_ENV=travis

notifications:
  webhooks: https://files-aopiaslhxw.now.sh

services:
  - docker --privileged

before_install:
  - docker-compose --version
  - docker-compose up -d
  - docker ps

install:
  - yarn install

script:
  - npm run lint
  - npm run test
